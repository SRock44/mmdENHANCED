/** Product branding (override with VITE_* in .env). */
export const appDisplayName = import.meta.env.VITE_APP_DISPLAY_NAME ?? "NewtonMMD";
/** Site / hosting slug (e.g. Firebase Hosting site id). */
export const siteId = import.meta.env.VITE_SITE_ID ?? "newtonmmd";
